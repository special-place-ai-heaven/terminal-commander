[package]
name = "terminal-commander-probes"
description = "Probe runners: process, file, PTY, directory, future journal/artifact probes."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
version.workspace = true
publish.workspace = true

[dependencies]
parking_lot = "0.12.5"
terminal-commander-core.workspace = true
terminal-commander-sifters = { version = "0.0.0", path = "../sifters" }
thiserror = "2.0.18"
tokio = { workspace = true, features = ["process", "macros", "rt-multi-thread", "io-util", "sync", "time", "fs"] }
vte = "0.15.0"

[lints]
workspace = true
