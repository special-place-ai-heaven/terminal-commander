{
  "request_example": {
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
      "name": "file_search",
      "arguments": {
        "path": "README.md",
        "query": "Terminal Commander",
        "case_insensitive": true,
        "max_matches": 10,
        "max_snippet_bytes": 160
      }
    }
  },
  "tool": "file_search",
  "_meta": {
    "schema_anchor": "TC05/mcp-tools/file_search-v1",
    "fixture_id": "contracts/mcp-tools/file_search.v1.json",
    "status": "live",
    "consumed_by": [
      "TC45"
    ],
    "note": "Daemon-backed bounded file search. Success returns capped snippets and never streams whole files. TB-10: path is a single regular file (not a directory walk) and query is a plain SUBSTRING (literal, not regex); both params carry descriptions naming this behavior so the first call matches as the caller expects."
  },
  "response_example": {
    "path": "README.md",
    "matches": [
      {
        "line": 3,
        "byte_offset": 44,
        "snippet": "# Terminal Commander"
      }
    ],
    "truncated": false,
    "bytes_scanned": 4096
  },
  "unavailable_error_example": {
    "message": "daemon_unavailable",
    "data": {
      "code": "daemon_unavailable",
      "message": "terminal-commanderd is not reachable",
      "details": {
        "status": "unavailable",
        "reason": "binary_not_found",
        "diagnostics": {
          "endpoint": {
            "kind": "unix_socket",
            "path": "/tmp/terminal-commanderd.sock"
          },
          "log_path": null,
          "last_error": "test: binary not found",
          "startup_attempted": false,
          "startup_elapsed_ms": 0
        }
      }
    }
  },
  "invariants": [
    "path and query are required",
    "query is a literal substring match, not a regex (TB-10)",
    "path targets a single regular file, not a directory (TB-10)",
    "max_matches and max_snippet_bytes are optional and daemon-clamped",
    "success returns bounded snippets, not whole files"
  ]
}
