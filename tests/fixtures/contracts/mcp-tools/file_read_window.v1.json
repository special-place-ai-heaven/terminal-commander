{
  "request_example": {
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
      "name": "file_read_window",
      "arguments": {
        "path": "README.md",
        "start_line": 1,
        "max_lines": 20,
        "max_bytes": 4096
      }
    }
  },
  "tool": "file_read_window",
  "_meta": {
    "schema_anchor": "TC05/mcp-tools/file_read_window-v1",
    "fixture_id": "contracts/mcp-tools/file_read_window.v1.json",
    "status": "live",
    "consumed_by": [
      "TC45"
    ],
    "note": "Daemon-backed bounded file read. It returns line windows, not whole files. TB-11: start_line is 1-based; the input schema declares minimum 1 and the description documents that a 0 is clamped up to 1 by the daemon (there is no line 0), so the 1-based prose and the schema agree."
  },
  "response_example": {
    "path": "README.md",
    "lines": [
      {
        "line": 1,
        "byte_offset": 0,
        "text": "# Terminal Commander"
      },
      {
        "line": 2,
        "byte_offset": 20,
        "text": ""
      }
    ],
    "file_bytes": 32768,
    "truncated": true,
    "next_byte_offset": 21
  },
  "unavailable_error_example": {
    "message": "daemon_unavailable",
    "data": {
      "code": "daemon_unavailable",
      "message": "terminal-commanderd is not reachable",
      "details": {
        "status": "unavailable",
        "reason": "binary_not_found",
        "diagnostics": {
          "endpoint": {
            "kind": "unix_socket",
            "path": "/tmp/terminal-commanderd.sock"
          },
          "log_path": null,
          "last_error": "test: binary not found",
          "startup_attempted": false,
          "startup_elapsed_ms": 0
        }
      }
    }
  },
  "invariants": [
    "path is required",
    "start_line is 1-based; the schema declares minimum 1 and a 0 is clamped to 1 (TB-11)",
    "start_line, max_lines, and max_bytes are optional and daemon-clamped",
    "success returns bounded lines and continuation offset, never a whole-file dump"
  ]
}
